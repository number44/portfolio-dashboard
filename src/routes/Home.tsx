import React from 'react';
import Layout from '../layouts/Layout';
import Box from '../layouts/Box';

interface PropsI {}
const Home = ({}: PropsI) => {
	return (
		<>
			<Box>Home Page</Box>
		</>
	);
};

export default Home;
